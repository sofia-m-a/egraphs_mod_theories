FXERROR

[ ] Set up a proper makefile or similar and split this tex into multiple files, etc

[ ] Title page fixes

[ ] Switched from IEEE to numeric cite style to get [1,2,3] cites like Nick wanted. Is this good?

[ ] Review floats

[x] Outdated UF:
 The resulting rewrite system is basically how UF is actually implemented in many cases (think \texttt{while e.next != e}) but as a rewrite system is not actually terminating: it is $\rightarrow \setminus 1$ that is terminating. Fix or explain
 Besides, why not pictures here? UF is not actually that important to understand as a rw system until (maybe) later

[-] The following is artificial but didactic. Finding a non-artificial \emph{and} didactic example would be nice, but is hard. But fix if I have some insight

[-] I cut the section `AC-closure in slightly more detail'. Rewrite this, maybe?

[-] Definition of critical pairs between different relations?

[x] For representation status, we ought to only need the LHS of rewrite rules, right? (No)

[-] Ref to hardness of AC-matching. But note ordinary E-matching is already NP-hard...

[x] Actually, the above needs much more fleshing out due to the annoyance of the need to use the AC-reductions on matches to get termination in the presence of cycles, which requires another use of Dickson's lemma. Of course this is again highlighting the connection between matching and closure, but it needs probably at least a whole section

[x] How hard would implementing ordering constraints in my current engine be? Maybe not that hard. Actually, there is maybe a nicer way to write patterns that generate such constraints behind the hood, I think. But that is a bit of scope creep.

[-] David's point: it may be faster but is it fast enough? See if I can beg someone for an existing example that is known not to saturate that might with my system}

[-] Polynomial functors/containers

[x] 6.4
[x] 7.1.2 literature on containers
[x] 7.1.3 monad law
[x] 7 proofread
[x] 7 conjecture
[x] 8 precedent for constrained matching